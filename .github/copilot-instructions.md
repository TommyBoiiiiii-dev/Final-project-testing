# Smart Nutrition project

- Keep the app runnable with Python standard-library tools where practical.
- Store nutrition label records in SQLite through `nutrition_app.py`.
- Keep frontend assets in `static/` and API routes under `/api/`.
